schema: http://localhost:4000/graphql
documents: ./graphql/gqls/*.gql
generates:
  types.ts:
    plugins:
      - typescript
    config:
      avoidOptionals: true
      constEnums: true
          
          